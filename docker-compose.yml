version: '3.9'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ferdus615/lms-backend
    container_name: lms-backend
    ports:
      - '3000:3000'
    env_file:
      - .env
    restart: unless-stopped
